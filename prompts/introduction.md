# Introduction Prompt

为当前岗位生成一条 BOSS 打招呼消息。

要求：

- 30-100 个中文字符为宜。
- 结构：岗位兴趣 + 一项直接相关的真实经历 + 沟通请求。
- 只引用 `user-profile.md`。
- 针对不同岗位选择不同证据，不一次罗列全部技能。
- 不写“精通”、大型平台、百万用户、大厂客户、云认证或未确认结果。
- 若岗位需要用户未具备的硬条件，返回不应发送的原因，不生成勉强匹配文案。

发送前仍须通过 `RiskGuard`；本 Prompt 只生成草稿。

